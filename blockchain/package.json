{
  "name": "sensor-reward-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for sensor reward system",
  "scripts": {
    "compile": "forge build",
    "deploy": "node scripts/deploy.js",
    "generate": "node scripts/generateNethereum.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "solc": "^0.8.20"
  }
}
